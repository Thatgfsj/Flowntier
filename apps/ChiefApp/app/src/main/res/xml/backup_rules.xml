<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!--
        v0.1.0 (event 000070): the chief app stores no
        persistent state (runtime is on the desktop, this app
        is a thin controller). Backup everything by default
        so we don't have to keep a denylist in sync with new
        fields.
    -->
    <include domain="sharedpref" path="." />
    <include domain="file" path="." />
</full-backup-content>
