# 000084 — 全链路 Trace Log + 重新打包

## 背景
主席分析：workflow 根本没有启动，日志只有 startup + "Secret revealed"，
说明请求没有进入 Orchestrator。怀疑安装包里的 sidecar 是旧二进制。

## 调查结论
1. **源码已正确**：server.rs 第137行已有 `first_pipe_instance(i == 0)` (event 000083)
2. **安装包 sidecar 是旧的**：Tauri build 在 04:22 运行，但之前 copy 的
   sidecar 可能不是最新编译版本
3. **日志只显示 event 000080**：说明运行的是旧 sidecar，没有 000082 的 phase log

## 修改内容
在调用链的**每一个节点**加入 `[TRACE]` 日志：

```
[Desktop] run_workflow Tauri command
  ↓
[Desktop] pipe_request: opening pipe / writing request / reading response
  ↓
[Pipe] rpc_listener: worker + first_pipe_instance flag + client connected
  ↓
[Pipe] serve_rpc_connection → handle_one: raw request + dispatch result
  ↓
[Dispatcher] dispatch: entering / exact match / pattern match / NOT_FOUND
  ↓
[Handlers] /api/run_workflow: entered / task validated / Orchestrator created
  ↓
[Handlers] orch.run() spawned on background tokio task
  ↓
[Orchestrator] Orchestrator::run() ENTERED
  ↓
[Orchestrator] phase=requirement → phase=plan → ... → phase=delivery
  ↓
[Orchestrator] workflow finished
```

修改文件：
- `crates/pipe-server/src/server.rs` — rpc_listener worker trace + handle_one trace
- `crates/pipe-server/src/dispatcher.rs` — dispatch 每个分支 trace
- `crates/pipe-server/src/handlers.rs` — /api/run_workflow handler trace
- `crates/pipe-server/src/orchestrator.rs` — Orchestrator::run() entry trace
- `crates/pipe-server/src/bin/flowntier-runtime.rs` — binary startup marker
- `apps/desktop/src-tauri/src/lib.rs` — pipe_request + run_workflow + events_bridge trace

## 构建
1. cargo build --release --target x86_64-pc-windows-gnu (sidecar)
2. cp sidecar → apps/desktop/src-tauri/binaries/
3. pnpm tauri build (desktop + NSIS installer)
4. node scripts/patch-nsis.cjs (taskkill + sidecar check)
5. makensis 重新打包

## 输出
- `C:/Users/thatg/Desktop/Flowntier_0.4.22_x64-setup.exe` (8.5MB)
- GitHub: commit 13d8a46

## 下一步
主席安装新包 → 运行 tarot 测试任务 → 检查日志中的 [TRACE] 标记
