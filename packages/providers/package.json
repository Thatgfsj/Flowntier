{
  "name": "@flowntier/providers",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Provider metadata tables (capabilities, context windows, costs). Read-only data, no SDK calls.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./models": "./src/models.ts"
  },
  "scripts": {
    "lint": "echo ok",
    "format": "echo ok",
    "format:check": "echo ok",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
